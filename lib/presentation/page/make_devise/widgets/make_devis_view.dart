import 'package:flutter/material.dart';

class MakeDevisView extends StatelessWidget {
  const MakeDevisView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
