import 'package:flutter/material.dart';

/// Dislpay contructor calculator view.
class CalculatorConstructorView extends StatelessWidget {
  /// Constructor of new [CalculatorConstructorView].
  const CalculatorConstructorView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Container();
}
