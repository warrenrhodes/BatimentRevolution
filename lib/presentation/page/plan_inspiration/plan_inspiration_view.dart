import 'package:flutter/material.dart';

class PlanInspirationView extends StatelessWidget {
  const PlanInspirationView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
